/** Re-exports for legacy imports — canonical implementation: `../data-generator.ts`. */
export {
    advanceLiveSeries,
    appendBarAfter,
    buildMockIntervals,
    jitterLastBar,
    LIVE_APPEND_EVERY,
    LIVE_TICK_MS,
    toHeikinAshi,
} from '../../data-generator';
export type {LiveTickCounter} from '../../data-generator';
